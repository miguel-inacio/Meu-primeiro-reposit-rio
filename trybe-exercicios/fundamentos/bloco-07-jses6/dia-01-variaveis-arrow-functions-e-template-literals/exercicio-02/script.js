const oddsAndEvens = [13, 3, 4, 10, 7, 2];

const sortedArray = array => (`Os números ${array.sort()} se encontram ordenados de forma crescente!`);

// Seu código aqui.

console.log(sortedArray(oddsAndEvens)); // será necessário alterar essa linha 😉